# Installer backups created by install.sh --force / per-file overwrite prompt
*.bak.[0-9]*

# Editor / OS noise
.DS_Store
*.swp
*~
.idea/
.vscode/

# Test artefacts
tests/tmp/

# Local installer simulation scratch (mock apps with their own nested .git)
simulation/

# Local AI/CLI tooling artefacts (per-machine, not for sharing)
.antigravitycli/
